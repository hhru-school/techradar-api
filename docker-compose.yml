version: "3.9"
services:
  postgres:
    container_name: postgres
    image: postgres:15.2
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
    ports:
      - "5433:5432"
    networks:
      - radar
  techradar-api:
    container_name: techradar-api
    image: techradar-api:1.0
    command: [
      "bash -c 'touch /app.jar'"
    ]
    ports:
      - "8080:8080"
    depends_on:
      - "postgres"
    networks:
      - radar

networks:
  radar:
    name: radar
    driver: bridge
